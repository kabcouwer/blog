Market Money is a Rails Rest API that provides CRUD (Create, Read, Update, Delete) 
functionality for managing markets and vendors. RSpec is used as the testing framework 
and Faker is used to generate fake data in order to ensure the functionality of endpoints.

- 100% test coverage
- JSON Serialization
- Error handling

export default ({children}) => <div className="prose prose-neutral">{children}</div>